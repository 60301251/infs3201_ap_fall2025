<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Search Photos</title>
    <link rel="stylesheet" href="/styles/gallery.css">

</head>
<body>
<div class="page-shell">

    <div class="top-bar">
        <a href="/">← Back to albums</a>

        {{#if user}}
            <div>
                <span style="margin-right: 10px;">Logged in as {{user.name}}</span>
                <a href="/logout">Logout</a>
            </div>
        {{/if}}
    </div>

    <h1 class="search-title">Search photos</h1>

    <form class="search-form" method="get" action="/search">
        <input
            type="text"
            name="q"
            placeholder="Search by title, description, or tags"
            value="{{searchTerm}}"
            required
        >
        <button type="submit">Search</button>
    </form>

    {{#if showNoResults}}
        <p class="summary">No public photos matched your search.</p>
    {{/if}}

    {{#if hasResults}}
        <p class="summary">Showing {{resultCount}} matching public photo(s).</p>
    {{/if}}

    <div class="photo-grid">
        {{#each photos}}
            <a class="photo-card" href="/photo/{{id}}">
                
                <img src="/photos/{{filename}}" alt="{{title}}">

                <div class="photo-info">
                    <h3>{{title}}</h3>
                    <p>{{description}}</p>
                </div>
            </a>
        {{/each}}
    </div>

</div>
</body>
</html>