<h1>{{album.name}}</h1>

<p>Photos in this album:</p>

{{#if photos.length}}
<ul>
    {{#each photos}}
        <li><a href="/photo/{{id}}">{{title}}</a></li>
    {{/each}}
</ul>
{{else}}
<p>No photos in this album yet.</p>
{{/if}}

<a href="/">Back to Albums</a>

<p>
    Logged in as: {{user.name}} | <a href="/logout">Logout</a>
</p>


