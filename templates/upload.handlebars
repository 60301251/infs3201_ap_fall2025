<h2>Upload Photo</h2>

{{#if photoId}}
    <p>Photo uploaded successfully!</p>
    <a href="/photo/{{photoId}}/edit">Click here to fill in details for the uploaded photo</a>
    <br><br>
{{/if}}

<form action="/album/{{album.id}}/upload" method="POST" enctype="multipart/form-data">
    <label>Photo:</label>
    <input type="file" name="photo" required />

    <!-- fields hidden/ignored by upload logic per assignment — shown only if you want -->
    <button type="submit">Upload</button>
</form>

<p><a href="/album/{{album.id}}">Back to Album</a></p>
